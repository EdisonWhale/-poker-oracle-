# AiPoker — UI/UX 设计规范

> 从 PRD v1.0 §B 抽取。桌面优先（1024px+），深色主题。

---

## 1. 信息架构

```
/                           ← Landing Page
├── /login
├── /register
├── /dashboard              ← 主控台（核心枢纽）
│   ├── /rooms              ← 房间管理
│   ├── /history            ← 对局历史
│   └── /profile            ← 个人资料/统计
├── /room/:id               ← 等待室
├── /game/:id               ← 牌桌（实时核心）
└── /replay/:handId         ← 回放查看器
```

---

## 2. 牌桌界面布局（1440px 基准）

```
┌──────────────────────────────────────────────────────────────────┐
│  ♠ AiPoker   房间: 训练室#1   盲注: 10/20   局数: #7      [⚙][✕]  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│           [Bot2 ●]            [Bot3 ●]                          │
│    [Bot1 ●]                              [Bot4 ●]               │
│                  ╔══════════════════╗                           │
│                  ║  ♠A  ♥K  ♣7     ║  ← 公共牌               │
│                  ║  底池: 340      ║                           │
│    [Bot5 ●]      ╚══════════════════╝      [ 空位 ]             │
│                                                                  │
│              ┌──────────────────┐                               │
│              │  [♥A ♦K]         │  ← 玩家手牌                 │
│              │  Edison  2,840   │                               │
│              │  ████░░░░  18s   │  ← 计时器                   │
│              └──────────────────┘                               │
├──────────────────────────────────────────────────────────────────┤
│ 行动历史: Bot1 raise 60 · Bot3 call · 你 3bet 180 · Bot1 call... │
├──────────────────────────────────────────────────────────────────┤
│  [弃牌(F)]  [跟注 60(C)]  [加注 ──────●────── 360]  [全压(A)]  │
│                                        [底池赔率: 28%] [提示▸]  │
└──────────────────────────────────────────────────────────────────┘
```

### 训练提示面板（右侧可折叠）

```
┌─────────────────────┐
│ 📊 训练提示         │
├─────────────────────┤
│ 手牌强度: Top 18%   │
│ 底池赔率: 28%       │
│ 建议胜率需求: >28%  │
│                     │
│ 位置: 按钮位(BTN)   │
│ 建议: ✅ 跟注/加注  │
│ [详细说明 ▾]        │
└─────────────────────┘
```

---

## 3. 关键页面

**Dashboard：**
- 左：用户卡（头像、筹码、今日统计）
- 中：最近对局列表（结果、净盈亏、"▶ 回放"按钮）
- 右：快速开始（一键创建默认房间）

**等待室（/room/:id）：**
- 座位布局（已加入玩家 + Bot 占位）
- Bot 配置（难度 / 数量）
- "开始游戏"按钮（房主可见）

**回放查看器（/replay/:handId）：**
```
┌──────────────── 回放界面 ──────────────────┐
│  [◀◀] [◀] [▶/⏸] [▶] [▶▶]  速度: 1x      │
│  ████████████████░░░░░░░  第13/31步       │
│                                            │
│  [牌桌状态（与游戏界面相同）]               │
│                                            │
│  ┌ 当前动作 ──────────────────────────┐    │
│  │ Edison: RAISE $180 (Pre-flop)      │    │
│  │ [✅ 好决策] [❌ 值得反思] [📝 笔记] │    │
│  └──────────────────────────────────┘    │
└──────────────────────────────────────────┘
```

---

## 4. 视觉设计

### 颜色系统（CSS Variables）

```css
--bg-primary:    #0D1117;  /* 主背景 */
--bg-surface:    #161B22;  /* 卡片/面板 */
--table-felt:    #1B5E20;  /* 牌桌桌布 */
--table-border:  #4A7C59;  /* 牌桌边框 */
--accent-gold:   #FFD700;  /* 底池/赢家 */
--text-primary:  #E6EDF3;
--text-muted:    #8B949E;
--error-red:     #EF5350;  /* 弃牌/危险 */
--success-green: #66BB6A;  /* 赢牌/正确 */
--chip-white:    #F5F5F5;
--chip-red:      #E53935;
--chip-blue:     #1E88E5;
--chip-green:    #43A047;
--chip-black:    #212121;
```

### 动画规范

| 动画 | 时长 | 缓动 | 说明 |
|------|------|------|------|
| 发牌 | 300ms | ease-out | 牌从牌堆飞向玩家位 |
| 翻牌 | 400ms | cubic-bezier | 3D Y轴翻转 |
| 下注 | 500ms | spring | 筹码移至底池 |
| 赢牌 | 600ms | ease-in-out | 底池飞向赢家 + 金色粒子 |
| 当前行动者 | infinite | pulse | 边框脉冲光效 |

---

## 5. 组件清单

| 组件 | 职责 |
|------|------|
| `PokerTable` | 椭圆形布局容器（2-9 座位自适应） |
| `Seat` | 玩家位：头像、筹码、当前下注、行动高亮 |
| `PlayingCard` | SVG 渲染，支持翻转动画、highlight |
| `CommunityCards` | 公共牌展示（Flop/Turn/River 渐次出现） |
| `HoleCards` | 底牌展示（仅自己可见；回放/showdown 展示） |
| `PotDisplay` | 主池/边池展示 |
| `ActionPanel` | fold/check/call/bet/raise + BetSlider |
| `BetSlider` | 金额输入（只允许合法区间：minBet..maxBet） |
| `ActionHistory` | 本手行动历史（可折叠） |
| `TimerBar` | 行动倒计时进度条 |
| `TrainingHUD` | 训练提示面板 |
| `ReplayControls` | 回放：播放/暂停/逐步/速度控制 |
| `ChipStack` | 筹码堆视觉（按面值分颜色） |

### 组件约束
- 渲染组件不直接依赖 socket（由 store/hook 供数据）
- 高频更新组件（TimerBar、动画）隔离状态，避免全桌重渲染
- 优先对高频更新/大组件使用 `React.memo`（其余按 profiling 决定，避免过度 memo）

---

## 6. 响应式

- MVP：桌面优先（1024px+）
- 断点：1440px（宽桌面）/ 1024px（普通桌面）
- 768px 以下：不在 MVP 范围，降级为"观看模式"

---

## 6.1 可见性契约（Visibility Contract）

> 该条款为牌桌交互硬约束，优先级高于视觉稿微调。

- 操作区（ActionPanel）在任何状态下（含 Raise 展开）不得遮挡以下信息：
  - 当前用户底牌
  - 当前用户倒计时（秒数+进度条）
  - 当前可执行动作的关键金额（Call / Raise / All-in）
- 牌桌页面必须采用 `Header + Table Arena + Action Dock` 三段式结构，禁止使用覆盖式 `fixed bottom` 操作栏压在牌桌内容上。
- 行动历史与训练提示应优先进入 Dock 或侧边区域，不得占用当前用户 seat 区域的视觉主路径。
- 视觉验收至少覆盖分辨率：`1366x768`、`1536x960`、`1728x1117`（常见 Mac/外接屏）。
- 验收标准：
  - 当前用户 seat 的底牌区域在视口内完整可见。
  - 倒计时最后 10 秒仍可稳定识别。
  - Raise 交互打开/关闭时不产生关键 UI 的遮挡跳变。

---

## 7. 快捷键

| 键 | 动作 |
|----|------|
| `F` | Fold |
| `C` | Call |
| `K` | Check |
| `R` | 聚焦 Raise 输入框 |
| `A` | All-in |
| `Space` | Check（若可）/ Call（若需） |
| `Enter` | 确认当前 Raise 金额 |
| `Esc` | 取消 Raise 输入 |
